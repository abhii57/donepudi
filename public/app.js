const state={token:localStorage.token||'',user:JSON.parse(localStorage.user||'null'),articles:[]};
const $=s=>document.querySelector(s); const toast=m=>{const t=$('#toast');t.textContent=m;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),2800)};
async function api(path,opts={}){const r=await fetch(path,{...opts,headers:{'Content-Type':'application/json',...(state.token?{Authorization:`Bearer ${state.token}`}:{})}});const d=await r.json();if(!r.ok)throw new Error(d.error||'Something went wrong');return d}
function updateAccount(){const a=$('#account');if(!state.user){a.innerHTML='<div class="account-actions"><button class="ghost" onclick="openAuth(\'login\')">Log in</button><button class="primary" onclick="openAuth(\'signup\')">Join PulseWire</button></div>';return}a.innerHTML=`<div class="account-actions"><div class="user-chip"><span class="avatar">${state.user.name[0].toUpperCase()}</span>${state.user.name.split(' ')[0]}</div>${state.user.role==='admin'?'<button class="primary" onclick="postModal.showModal()">Publish +</button>':''}<button class="ghost" onclick="logout()">Log out</button></div>`}
function story(a){const date=new Date(a.published_at).toLocaleDateString('en',{month:'short',day:'numeric'});return `<article class="story"><img class="cover" src="${a.image_url||'https://images.unsplash.com/photo-1504711434969-e33886168f5c?auto=format&fit=crop&w=900&q=85'}" alt="" onerror="this.style.display='none'"><div class="story-content"><div class="meta">${a.category.toUpperCase()} · ${date}${a.is_breaking?'<span class="breaking">● BREAKING</span>':''}</div><h3>${a.title}</h3><p>${a.excerpt}</p><div class="story-footer"><span>By ${a.author}</span><div class="actions"><button class="${a.liked?'liked':''}" onclick="like(${a.id})">♥ ${a.likes}</button><button onclick="comments(${a.id},'${a.title.replaceAll("'","\\'")}')">◌ ${a.comments}</button></div></div></div></article>`}
async function load(){try{state.articles=await api('/api/articles');$('#feedGrid').innerHTML=state.articles.map(story).join('')}catch(e){$('#feedGrid').innerHTML='<p class="loading">Unable to reach the newsroom. Check your database connection.</p>'}}
function openAuth(mode){authModal.showModal();renderAuth(mode)}
function renderAuth(mode){$('#authContent').innerHTML=`<div class="auth-tabs"><button class="${mode==='login'?'active':''}" onclick="renderAuth('login')">Log in</button><button class="${mode==='signup'?'active':''}" onclick="renderAuth('signup')">Create account</button></div><form onsubmit="submitAuth(event,'${mode}')"><h2>${mode==='login'?'Welcome back':'Join the conversation'}</h2>${mode==='login'?'<p class="demo">Editor demo: admin@pulsewire.test / Admin123!</p>':'<input name="name" placeholder="Your name" required>'}<input name="email" type="email" placeholder="Email address" required><input name="password" type="password" placeholder="Password" minlength="6" required><button class="primary">${mode==='login'?'Log in':'Create account'} →</button></form>`}
async function submitAuth(e,mode){e.preventDefault();try{const data=Object.fromEntries(new FormData(e.target));const r=await api('/api/auth/'+(mode==='login'?'login':'signup'),{method:'POST',body:JSON.stringify(data)});state.token=r.token;state.user=r.user;localStorage.token=r.token;localStorage.user=JSON.stringify(r.user);authModal.close();updateAccount();load();toast(`Welcome, ${r.user.name.split(' ')[0]}!`)}catch(e){toast(e.message)}}
function logout(){localStorage.clear();state.token='';state.user=null;updateAccount();load();toast('You are logged out.')}
async function like(id){if(!state.user)return openAuth('login');try{await api(`/api/articles/${id}/like`,{method:'POST'});load()}catch(e){toast(e.message)}}
async function comments(id,title){if(!state.user)return openAuth('login');const content=prompt(`Comment on “${title}”`);if(!content)return;try{await api(`/api/articles/${id}/comments`,{method:'POST',body:JSON.stringify({content})});toast('Comment added.');load()}catch(e){toast(e.message)}}
$('#postForm').onsubmit=async e=>{e.preventDefault();try{const d=Object.fromEntries(new FormData(e.target));d.isBreaking=!!e.target.isBreaking.checked;await api('/api/articles',{method:'POST',body:JSON.stringify(d)});postModal.close();e.target.reset();load();toast('Story published to the feed.')}catch(e){toast(e.message)}};
document.querySelectorAll('.filters button').forEach(b=>b.onclick=()=>{document.querySelector('.filters .active').classList.remove('active');b.classList.add('active');const name=b.textContent;$('#feedGrid').innerHTML=state.articles.filter(a=>name==='All'||a.category===name).map(story).join('')||'<p class="loading">No stories in this section yet.</p>'});updateAccount();load();setInterval(load,30000);
